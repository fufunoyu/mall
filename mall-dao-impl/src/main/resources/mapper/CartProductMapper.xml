<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.rhinoceros.mall.dao.dao.CartProductDao">
    <!-- 找到购物车-->
    <select id="findByUserId" resultType="CartProduct" parameterType="Long">
        SELECT *
        FROM cart_product
        WHERE user_id = #{userId}
    </select>

    <!-- 浏览商品消息-->
    <select id="findByProductId" resultType="Product" parameterType="Long">
        SELECT *
        FROM product
        WHERE id = #{productId}
    </select>

    <!--删除商品消息-->
    <select id="deleteByCartProductId" resultType="CartProduct" parameterType="Long">
        DELETE
        FROM cart_product
        WHERE id = #{cartProductId}
    </select>
</mapper>